#include <stdio.h>
/**
 *main - Entry point
 *Print random numbers
 *Return: Always 0 (Success)
 */
int main(void)
{
	for (char alpha = 'a'; alpha <= 'z'; alpha++)
	{
		putchar(alpha);

	}
	putchar("\n");
	return (0);
}
